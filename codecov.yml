ignore:
  # In-memory HTTP server used only to support frontend integration tests.
  - core-api/internal/testserver/**

coverage:
  status:
    project:
      frontend:
        flags:
          - frontend
        target: auto
        threshold: 0%
        informational: true
